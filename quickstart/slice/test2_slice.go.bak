package main

import "fmt"

func printArray(myArray []int) {
	for _, value := range myArray {
		fmt.Println("value = ", value)
	}
}

func main() {
	myArray1 := []int{11, 22, 33, 44} //动态数组，slice切片

	fmt.Printf("myArray1 type is %T\n", myArray1)

	printArray(myArray1)
}
